% =========================
% Preamble.tex (simple + robust)
% =========================

% Language (APA mapping)
\usepackage[american]{babel}
\usepackage{csquotes}

% Page layout (A4 + sane margins)
\usepackage[a4paper,margin = 2.5cm]{geometry}
\usepackage[final]{microtype} %Use if footnotes are used

% Paragraph formatting (no indent + space between paragraphs)
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\usepackage{setspace}
\setstretch{1.168}

% Math
\usepackage{amsmath}
\usepackage{amssymb}

% Figures
\usepackage{graphicx}
\graphicspath{
    {Inputs/Figures/}
    {Inputs/Pictures/}
}
\usepackage{subcaption}

% Tables (good looking + better control)
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}   % tables that auto-fit width
\usepackage{adjustbox}  % scale/fit tables to page
\usepackage{float}
\usepackage{threeparttable}

% Links (load late)
\usepackage[hidelinks]{hyperref}

% Bibliography (APA 7) — requires biber
\usepackage[backend=biber,style=apa]{biblatex}
\DeclareLanguageMapping{american}{american-apa}
\addbibresource{References.bib}

\usepackage[none]{hyphenat}
\sloppy

%Header with candidate number
% =======================
\usepackage{fancyhdr}
\pagestyle{fancy}

\fancyhf{} % nullstill

\fancyhead[L]{Candidate number: 74, 39}

\fancyfoot[C]{\thepage}

\renewcommand{\headrulewidth}{0.4pt}
